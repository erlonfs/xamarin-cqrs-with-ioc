<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Poc.MobileApp.Views.HomePage" Title="Gestao de Pessoas">

	<Grid Margin="0" Padding="5,0" ColumnSpacing="0" RowSpacing="5">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		
		<Button Grid.Row="0" Text="Consultar" Command="{Binding IrParaConsultar}"/>
		<Button Grid.Row="1" Text="Criar" Command="{Binding IrParaCriar}"/>
		<Button Grid.Row="2" Text="Sincronizar" Command="{Binding Sincronizar}"/>

	</Grid>

</ContentPage>
